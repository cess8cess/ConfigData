spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL5Dialect"
  database: 
    driverClassname: "com.mysql.jdbc.Driver"
  datasource:
    url: jdbc:mysql://localhost:3306/itemsharing?useUnicode=yes&characterEncoding=UTF-8
    username: pikacu
    password: 12345678
    test-while-idle: "true"
    validation-query: "SELECT 1"
server:
  port: 8082


    
